// next = document.querySelectorAll('.next')
// Object.entries(next).map(s => s[1].classList.remove('next'))
// Object.entries(next).map(s => s[1].classList.add('no-r-border'))


// prev = document.querySelectorAll('.prev')
// Object.entries(prev).map(s => s[1].classList.remove('prev'))